# 版本上限是必要的，不要改回純 >=：
#   - langchain 1.x 移除了 langchain.chains / langchain.memory /
#     langchain.prompts / langchain.text_splitter，rag_system.py 與
#     document_loader.py 會直接 ModuleNotFoundError。
#   - gradio 5/6 移除了 Chatbot(type="tuples")，app.py 會 TypeError。
# 以下為實測可跑的組合（Python 3.9–3.11）。

# --- LangChain (0.3.x，勿升到 1.x) ---
langchain>=0.3.30,<0.4
langchain-core>=0.3.86,<0.4
langchain-community>=0.3.31,<0.4
langchain-openai>=0.3.35,<0.4
langchain-text-splitters>=0.3.11,<0.4

# --- OpenAI ---
openai>=2.0,<3
tiktoken>=0.13,<0.14

# --- 向量資料庫 ---
qdrant-client>=1.16,<2

# --- 文件載入與處理 ---
pypdf>=6.0,<7
unstructured>=0.18,<0.26
docx2txt>=0.8
python-docx>=1.1
pandas>=2.0,<3
numpy>=2.0,<3
openpyxl>=3.1
xlrd>=2.0

# --- Web UI (4.x，勿升到 5/6) ---
gradio>=4.44,<5
gradio_client>=1.3,<2
# gradio 4.x 的相依上限，實測踩過的兩個坑：
#   - huggingface_hub 1.x 移除了 HfFolder，gradio/oauth.py 會 ImportError。
#   - starlette 1.x 移除了 TemplateResponse(name, context) 舊簽名，
#     gradio/routes.py 會在渲染頁面時 TypeError: unhashable type: 'dict'（頁面卡在 Loading）。
huggingface-hub>=0.25,<1
starlette>=0.41,<1
fastapi>=0.115,<0.130
uvicorn>=0.30,<1

# --- 選用：torch 僅用於 app.py 的 torch.cuda.empty_cache()（約 2.5GB）。
#     app.py 的 import 已改為可選，不裝也能正常運行。
# torch>=2.0.0
# torchvision>=0.15.0
# torchaudio>=2.0.0

# --- 本地 Ollama 後端（LLM_PROVIDER=ollama 時需要）---
#     另需自行安裝 Ollama 本體並 pull 模型，詳見 README。
langchain-ollama>=0.2,<1
